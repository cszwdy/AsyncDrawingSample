# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'AsyncDrawing' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AsyncDrawing
pod 'AsyncDisplayKit', '~> 1.9.73'
pod 'YYKit', '~> 1.0.5'

  target 'AsyncDrawingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AsyncDrawingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
